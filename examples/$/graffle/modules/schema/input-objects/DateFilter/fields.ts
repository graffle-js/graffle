import type { Schema as $Schema } from '../../$.js'

export interface gte {
  kind: 'InputField'
  name: 'gte'
  inlineType: [0]
  namedType: $Schema.Date
}

export interface lte {
  kind: 'InputField'
  name: 'lte'
  inlineType: [0]
  namedType: $Schema.Date
}
